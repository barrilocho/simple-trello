# Simple trello

Simple trello allows you to create and complete task, in just few steps. Is a very common type of organization, but sometimes a little complicated, Simple trello keeps this flow easy.
## Getting Started

First, run the development server:

```bash
npm run dev
# or
yarn dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

